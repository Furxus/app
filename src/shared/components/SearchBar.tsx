import TextField from "@mui/material/TextField";

const SearchBar = () => {
    return (
        <div className="flex items-center">
            <TextField placeholder="Search... (not functional yet) 🥲" />
        </div>
    );
};

export default SearchBar;
