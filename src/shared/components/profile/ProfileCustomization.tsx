import { Stack, Typography } from "@mui/material";

const ProfileCustomization = () => {
    return (
        <Stack>
            <Stack className="bg-neutral-700 p-4 rounded-lg w-"></Stack>
        </Stack>
    );
};

export default ProfileCustomization;
